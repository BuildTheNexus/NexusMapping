<!--
  This page is now handled by the server-side redirect in `+page.server.ts`.
  Authenticated users are sent to `/dashboard/map`, and unauthenticated users
  are sent to `/login`. This component will not be rendered.
-->
<div class="p-8">
	<p>Redirecting...</p>
</div>