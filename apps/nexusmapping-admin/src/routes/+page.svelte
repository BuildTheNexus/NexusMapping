<!--
  This page is never rendered.
  The server logic in `+page.server.ts` at this level will always
  redirect the user to either `/login` or `/dashboard`.
-->
<div class="p-8">
	<p>Redirecting...</p>
</div>